

## 2026-09-03 修好安裝鈕（injector 樣板缺 </script>）＋補齊 apple-touch 專用檔

**已部署 production `e5277be2`**（lightcatcher.pages.dev）。

`ridgeline-branding` 的 `install_inject.py` 樣板從頭到尾沒有 `</script>`——注入後那個
script 永不關閉，`</body></html>` 連同後面所有內容都被瀏覽器當成 JS 吞掉，
「📲 安裝到桌面」整塊語法錯誤、靜默失效。**注入器只印 ok，不會告訴你。**
線上實測 HTML 開=9 閉=8 確認中鏢（403 daytrail、405 jptrail 同樣中鏢）。
樣板已於 2026-09-03 修好，本次重跑注入器（冪等）→ 9/9 平衡。

順帶補齊兩件品牌檢核長期 FAIL：
- **正式檔缺 `lightcatcher-180.png`**：App 端本來就有一張 180 RGB 不透明，
  已用官方 1024 母檔照同一條管線（LANCZOS→180＋壓平在 `#A64B38`）重產比對，
  平均像素差 0.046、最大單點差 5 ＝同一條管線產的，所以直接把 App 端那張回填正式檔，
  保留已上線的 byte，使用者看到的圖不變。
- **apple-touch link 檔名不合家族慣例**：原本指 `./icon-180.png`。功能其實正常
  （線上實測回 200 image/png 8574 bytes，且 `icon-180` 不在 manifest 裡，
  沒有違反 CLAUDE.md 規則 9），但品牌檢核 B3 要求檔名就叫 `apple-touch-icon.png`，
  所以永遠 FAIL。**永遠亮紅燈的守門下一步就是被關掉，比沒有守門更糟**——
  故新增專用 `apple-touch-icon.png`（＝正式檔 180 的 byte 複本）並改指過去。

`sw.js`：ASSETS 加入 `./apple-touch-icon.png`，CACHE `v25`→`v26`。

驗收：4 段 inline script 全部通過 `new Function` 實際語法解析（不是正則猜）；
品牌檢核 **2 FAIL → 0 FAIL**（0 FAIL / 3 WARN / 19 PASS）；staging 20 檔、ASSETS 15 項到齊、無外洩。
部署前 baseline 已查（production 16 小時前、無並行動作）。
線上實測：script 9/9、安裝鈕區塊正確閉合、`</body></html>` 完整、
`/apple-touch-icon.png` 回 200 image/png 8574 bytes（與本機一致，不再是 SPA fallback 的 HTML）。


## 2026-09-03 · 分享預覽 OG 視覺系統（Batch 3 · 已部署）
> 只碰 icon 顯示品質與分享預覽：沒有改 UI、Layout、功能、routing、API、資料模型、品牌名稱、App Symbol。

- 部署：Cloudflare Pages deployment `7afba62a`，`--branch=main`。線上 https://lightcatcher.pages.dev/
- **baseline 政策**：不以 git HEAD 為基底。先用 `wrangler pages deployment list` 取得當時的 production deployment，把每個檔從那個釘死的 deployment URL 抓回來、逐檔存 SHA-256，再在那份上疊改動。
- 改動：`og-lightcatcher.png`（新增 1200×630）、`index.html`（head 補 canonical／description／og:*／twitter:*，全絕對網址）
- 非目標檔部署後逐檔重抓比對，全部與 baseline SHA-256 一致；`sw.js` byte 未動＝不觸發更新橫幅
- Icon：icon 像素檢查全 PASS（四角 #A64B38 不透明），**本次未動任何 icon**。
- 淡背景 pattern：無。本支沒有可用的本機真實資料集（預測／查詢都走 runtime API），依規範留白
- ⚠ 換圖要同步改 `og:image` 的 `?v=`（目前 `20260903-1`）
- ⚠ 本次改動尚未 git commit，列入「Production → Git Reconciliation」待辦
