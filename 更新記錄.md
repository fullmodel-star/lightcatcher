

## 2026-09-03 · 分享預覽 OG 視覺系統（Batch 3 · 已部署）
> 只碰 icon 顯示品質與分享預覽：沒有改 UI、Layout、功能、routing、API、資料模型、品牌名稱、App Symbol。

- 部署：Cloudflare Pages deployment `7afba62a`，`--branch=main`。線上 https://lightcatcher.pages.dev/
- **baseline 政策**：不以 git HEAD 為基底。先用 `wrangler pages deployment list` 取得當時的 production deployment，把每個檔從那個釘死的 deployment URL 抓回來、逐檔存 SHA-256，再在那份上疊改動。
- 改動：`og-lightcatcher.png`（新增 1200×630）、`index.html`（head 補 canonical／description／og:*／twitter:*，全絕對網址）
- 非目標檔部署後逐檔重抓比對，全部與 baseline SHA-256 一致；`sw.js` byte 未動＝不觸發更新橫幅
- Icon：icon 像素檢查全 PASS（四角 #A64B38 不透明），**本次未動任何 icon**。
- 淡背景 pattern：無。本支沒有可用的本機真實資料集（預測／查詢都走 runtime API），依規範留白
- ⚠ 換圖要同步改 `og:image` 的 `?v=`（目前 `20260903-1`）
- ⚠ 本次改動尚未 git commit，列入「Production → Git Reconciliation」待辦
